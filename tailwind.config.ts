/** @type {import('tailwindcss').Config} */
const config = {
  theme: {
    extend: {
      colors: {
        primary: "#002e6d",
        accent: "#f59e0b",
        surface: "#f8f9fa",
        "text-base": "#1c1c1c",
        muted: "#6b7280",
      },
    },
  },
}

export default config
